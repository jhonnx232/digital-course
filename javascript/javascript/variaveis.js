//variaveis   (diferente do c++ e de outras linguagens, o " ; " não é necessário)
let idade = 26//primeira variavel

console.log(idade, typeof idade)

//tipos de variaveis ; (no javascript não já diferença ente float e number)

idade = 30   //number
let numero = 20 //number
let altura= 1.8 //number
let nome = "jhonnX" // string(não há diferença entre string e char no javascript, amobs funcionam com "" e '')
let estudando = true //booleano

console.log(numero, typeof numero)
console.log(altura, typeof altura)
console.log(nome, typeof nome)
console.log(estudando, typeof estudando)

let outra;  //undefyned, como o próprio nome diz , variavel não definida (aparece no console)
let nula= null // variavel nula.

var level = 30 // voce pode declarar variavel com 'var' ao invés de 'let' :) *var é mais antigo... utilize o let ..

const constante = 100 // valor único e inalterável(let vem de "let ti change"/ deixe que mude)
